const User = require('./User');
const Profile = require('./Profile');
const Entry = require('./Entry');
const Benchmark = require('./Benchmark');
const Benchmark = require('./Payment');

module.exports = {
  User,
  Profile,
  Entry,
  Benchmark,
  Payment,
};
